# Xcode / build
build/
DerivedData/
*.xcuserstate
xcuserdata/
*.xcscmblueprint
*.xccheckout
*.moved-aside

# macOS
.DS_Store

# Create ML training project — 130MB checkpoints, exceeds GitHub's file limit.
# The exported model (kemon/KemonEmotionClassifier.mlmodel) IS committed.
mlproject/
